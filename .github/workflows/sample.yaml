name: sample pipeline
on: 
  push: 
  workflow_dispatch:

env:
  key:  value

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v3
    - uses: actions/setup-node@v4
      with:
        nodejs: '20.x'
    - name: run commands
      run: npm ci

